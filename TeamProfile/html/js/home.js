const options = {
    strings: ['Frontend Developer' ],
    typeSpeed: 190,
    backSpeed: 150,
    backDelay: 150,
    loop: true
  };
  
  const typed = new Typed('.multiple-text', options);

  const options2 = {
    strings: ['Frontend Developer' ],
    typeSpeed: 100,
    backSpeed: 100,
    backDelay: 100,
    loop: true
  };
  
  const typed2 = new Typed('.multiple-text2', options);